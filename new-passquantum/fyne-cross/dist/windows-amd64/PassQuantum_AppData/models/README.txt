# Place ONNX model files here before building with advanced-auth support.
#
# Required files:
#   face_model.onnx   — face_model short-range face detector (128×128 input)
#   mesh_model.onnx   — MediaPipe Face Mesh (468 landmarks, 192×192 input)
#
# Source: Convert or download from the MediaPipe/ONNX model repositories.
# CPU-only inference via gocv (OpenCV DNN module) — no GPU required.

